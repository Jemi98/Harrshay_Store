{
  /*import { Grid } from "@mui/material";

import { Box } from "@mui/system";
import React from "react";
import image from "../../../assets/img1.jpg";

const Layout = () => {
  return (
    <Grid container columnGap={2} mt={2} justifyContent="center">
      <Grid item lg={6} md={4} sm={10}>
        <Box sx={{ maxHeight: "630px", alignItems: "start" }}>
          <img style={{ maxHeight: "630px", maxWidth: "100%" }} src={image} />
        </Box>
      </Grid>
      <Grid item lg={5.5} md={4} sm={10}>
        <Box>
          <Box sx={{ display: "flex", justifyContent: "space-between" }}>
            <Box sx={{ maxHeight: "280px" }}>
              <img
                style={{ maxHeight: "280px", maxWidth: "320px" }}
                src={image}
              />
            </Box>

            <Box sx={{ maxHeight: "280px" }}>
              <img
                style={{ maxHeight: "280px", maxWidth: "320px" }}
                src={image}
              />
            </Box>
          </Box>

          <Box sx={{ pt: 2, maxHeight: "300px" }}>
            <img style={{ maxHeight: "300px", maxWidth: "100%" }} src={image} />
          </Box>
        </Box>
      </Grid>
    </Grid>
  );
};

export default Layout;*/
}
